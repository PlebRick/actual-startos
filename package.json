{
  "name": "actual-startos",
  "version": "0.1.0",
  "description": "StartOS package for Actual Server",
  "main": "index.js",
  "scripts": {
    "build": "npm run prettier && tsc",
    "prettier": "prettier --write startos/**/*.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@start9labs/start-sdk": "^0.3.6-beta.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "prettier": "^3.3.3",
    "@types/node": "^18.16.0"
  },
  "prettier": {
  "trailingComma": "all",
  "tabWidth": 2,
  "semi": false,
  "singleQuote": true
}
}